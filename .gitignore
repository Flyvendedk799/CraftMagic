node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example

# anthropic spend ledger
.spend/
.env.local

# mod / gradle
mod/build/
mod/.gradle/
mod/run/

# registry generation working dir (large jars, never committed)
tools/registry-gen/work/

# exported schematics / scratch output
/out/


# local postgres data
.pgdata/

# editor
.vscode/
.idea/
*.log

# Agent worktrees, created by the harness inside the repo.
.claude/

# Throwaway patch scripts. I write these to make a precise multi-line edit and delete them
# straight after; two have reached main by being forgotten between the write and the commit.
tools/_*.cjs
tools/_*.mjs
